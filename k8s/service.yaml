apiVersion: v1
kind: Service
metadata:
  name: webapp
  namespace: development
spec:
  type: ClusterIP  # Pode ser ClusterIP, já que o Ingress vai expor
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 3000
