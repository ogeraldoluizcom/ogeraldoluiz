apiVersion: v1
kind: Service
metadata:
  name: ogeraldoluiz
  namespace: development
spec:
  type: ClusterIP  # Pode ser ClusterIP, já que o Ingress vai expor
  selector:
    app: ogeraldoluiz
  ports:
    - port: 80
      targetPort: 3000
